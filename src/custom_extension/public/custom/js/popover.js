$(function() {
    $('.icon-popover').popover({
        trigger: 'hover',
        placement: 'top',
        html: true,
        content: '<strong>Markdown</strong> is a plain text formatting syntax designed so that it can be converted to HTML using a tool by the same name. Markdown is often used to format readme files, for writing messages in online discussion forums, and to create rich text using a plain text editor.'
    });
});